# VLM / judge backend (OpenAI-compatible)
DR_API_KEY=
DR_MODEL=
DR_BASE_URL=
DR_TEMPERATURE=0.0
DR_BACKEND=react          # ReAct loop (LangGraph StateGraph) — the only paradigm

# agent loop tuning
DR_MAX_RETRIES=2
DR_RETRY_CONF_FLOOR=0.6
DR_VERBOSE=true
